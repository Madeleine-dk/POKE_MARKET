<body class="background">
  <div class="login text-center m-2">
<div class="floating-image-container">
      <%= image_tag "clipart389400.png", width: 150 %>
    </div>
  <h2 class="text-center"style="margin-top: -27px;"><%= image_tag "pokemoncardtext.png", width: 300, height: 100 %></h2>
  </div>

  <div class="container">
    <div class="row">
      <% @cards.each do |card| %>
        <div class="col-md-3 mb-4">
          <%= link_to card_path(card) do %>
            <div class="card-body-index h-100 py-3 px-3">
              <div class="card-body">
                <div class ="img-fluid">
                <%= cl_image_tag card.photo.key, width: 400, crop: :fill, class:"index_images" %>
                </div>
                <div>
                  <h3 class="card-title text-center my-2" id="card-title-color"><strong><%= card.title %></strong></h3>
                  <p class="card-text"><strong>Details:</strong> <%= card.description %></p>
                  <p class="card-text"><strong>Price:</strong> <strong><%= card.ppd %>€</strong> per day</p>
                  <p class="card-text"><strong>Vendor:</strong> <%= card.user.username %></p>
            <% end %>
                </div>
              </div>
            </div>
        </div>
      <% end %>
    </div>
  </div>
</body>

</body>
